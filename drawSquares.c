/*write a program that will ask the user
  for a value and then 
  draw a square of that size.

   for example if user enters 3
   program draws:
    
    ***         ===
    ***  (or)   | |
    ***         ===

*/
/*this function asks the user to enter a positive number
  and return it.  It will validate that the value was a positive
  number*/

int getSize(void){
   int inputData;
   /*put code here to read a number into inputData*/
   return inputData;
}
/*this function draws a square*/
void draw(int size){
  /*put code here to draw a square*/
}
int main(void){
  int size;
  size = getSize();
  draw(size);
}
