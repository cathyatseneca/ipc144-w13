/*
  this function will pass back the first and last digit of value through
  the pointers first and second
*/
void getFirstLastDigit(int value, int* first, int* second);
int main(void){
  int firstdigit, lastdigit;
}
